# ── Build / Gradle / IDE ──
*.iml
.gradle/
/.idea/
/.kotlin/
.DS_Store
/build/
**/build/
/captures/
.externalNativeBuild/
.cxx/
*.hprof
/out/

# ── Local config ──
/local.properties

# ── Secrets & signing — NEVER commit ──
*.jks
*.keystore
keystore.properties
signing.properties
*.p12
*.key
secrets.properties
*.secrets
secring.gpg
.env
*.env
# Firebase app config (app-level; only if a sample/app module vendors it)
**/google-services.json
